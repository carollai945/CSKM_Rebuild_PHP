Feature: 業務招生與學員服務流程
  As a 業務人員、主管或 CEO
  I want to 透過名單查詢、指派、訪談與學員狀態頁面完成招生追蹤
  So that 招生到成單的流程能被完整管理

  Background:
    Given 電訪總覽頁提供查詢欄位、名單清單、新名單表單與派發按鈕
    And 電訪總覽頁可查詢名單並將勾選名單派發給指定學顧
    And 訪談記錄頁提供日期選擇器，查詢後會先清空舊列表再重建結果
    And 學員狀態頁提供服務記錄、繳費記錄與課程狀態等入口按鈕

  Scenario: 業務查詢電訪總覽
    Given 使用者已登入
    When 使用者在 B00 填入姓名、Email、匯入日期或派發日期等條件
    And 觸發 Vue 的查詢動作
    Then 前端送出名單查詢請求
    And 名單清單與右側明細區塊會同步更新

  Scenario: 主管只有在選取名單且指定學顧後才能派發
    Given 使用者已登入 B00
    When 尚未勾選任何名單或尚未選擇學顧
    Then 派發按鈕保持不可按
    When 主管勾選至少一筆名單且選好學顧
    Then 派發按鈕變為可按
    And 前端送出派發請求

  Scenario: 業務建立新潛在學員名單
    Given 使用者已進入 B00 新名單區塊
    When 使用者填寫生日、建檔日期、下次聯繫日期等欄位
    Then 日期欄位使用 datepicker 或 date input 控制輸入格式
    And 新名單資料可被儲存為潛在學員

  Scenario: 訪談記錄入口頁以多條件 Ajax 查詢並重建表格
    Given 使用者已進入訪談記錄入口頁
    When 使用者以姓名、電話、Email、匯入日期或派發日期按下查詢
    Then 前端送出訪談記錄查詢
    And 先刪除舊資料列
    And 再將回傳資料逐列加入表格
    And 同步更新清單筆數

  Scenario: 使用者從訪談記錄入口建立新訪談資料
    Given 使用者已在 B01 搜尋結果中勾選一筆名單
    When 使用者按下新增訪談記錄
    Then 畫面會帶著目前勾選的名單進入訪談明細頁

  Scenario: 學員狀態頁依條件查詢並進入後續服務作業
    Given 使用者已進入學員狀態頁
    When 使用者輸入學員姓名、電話或選擇機構後按下查詢
    Then 前端送出學員狀態查詢
    And 畫面重建學員狀態清單
    And 使用者可再進入服務記錄、繳費記錄或課程狀態子頁

  Scenario: 非 CEO 僅能管理本區域資料
    Given 使用者沒有 CEO 權限
    When 使用者嘗試查詢跨區域名單或學員
    Then 查詢結果只會顯示其授權範圍內的名單或學員
    And 前端僅顯示可管理的名單與派發對象
