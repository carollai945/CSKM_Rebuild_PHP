Feature: B00 電訪名單查詢與派發
  As a 業務或主管
  I want to 依身份查詢名單、建立新名單或派發給學顧
  So that 招生名單能被有效追蹤

  Background:
    Given 電訪總覽頁以 Vue 維護查詢條件、名單清單、勾選結果與新增名單視窗
    And 電訪總覽頁提供姓名、Email、匯入日期、派發日期、區域、學顧、建立者與是否已派發等查詢欄位
    And 電訪總覽頁提供名單清單、逐筆編輯按鈕、逐筆刪除按鈕、全選、全不選、學顧選單、派發按鈕、批次刪除按鈕與新增名單視窗

  Scenario: 頁面載入時會先取得預設選單與角色相關預設值
    Given 使用者第一次進入電訪總覽頁
    When Vue 完成初始化
    Then 畫面會先載入區域、學顧、建立者、性別、來源與等級選單
    And 建檔日期迄日、派發日期迄日與新增名單的建檔日期都會預設為今天
    And 若使用者不是主管，區域、學顧、建立者與派發對象都會預設為本人
    And 若使用者不是主管，是否已派發預設為已派發

  Scenario: 業務依條件查詢自己可處理的名單
    Given 使用者身份為業務
    And 使用者已進入電訪總覽頁
    When 使用者填入查詢條件後按下查詢
    Then Vue 會把目前查詢欄位保存為本次查詢條件
    And 名單清單與明細區塊會同步更新
    And 清單只顯示該業務可處理的名單

  Scenario: 使用者切換顯示筆數後重新查詢
    Given 使用者已進入電訪總覽頁
    When 使用者將顯示筆數改為 50、100 或全部後按下查詢
    Then 名單清單會依新的顯示筆數重新載入

  Scenario: 全選與全不選會直接改變目前勾選名單
    Given 使用者已查出多筆名單
    When 使用者按下全選
    Then 目前查詢結果中的所有名單都會被加入勾選清單
    And 派發與批次刪除會以這批勾選名單為作用對象
    When 使用者再按一次全選或按下全不選
    Then 所有勾選名單都會被清空

  Scenario: 主管選取名單且指定學顧後才能派發
    Given 使用者身份為主管
    And 主管已進入電訪總覽頁
    When 尚未勾選名單或尚未選擇學顧
    Then 派發按鈕保持不可按
    When 主管勾選至少一筆名單並選好學顧
    Then 派發按鈕變為可按
    When 主管確認派發
    Then 所有勾選名單會分配給選定學顧
    And 畫面會重新查詢並顯示最新派發結果

  Scenario: 主管取消派發確認時不得變更名單
    Given 使用者身份為主管
    And 主管已勾選至少一筆名單並選好學顧
    When 主管按下派發但取消確認視窗
    Then 派發動作不得送出
    And 名單維持原本指派狀態

  Scenario: 業務建立新潛在學員名單
    Given 使用者身份為業務
    And 使用者已開啟新增名單視窗
    When 使用者填寫姓名、性別、手機、學歷、來源、等級、生日、建檔日期與下次聯繫日期等欄位後按下儲存
    Then 這筆名單會被建立為新的潛在學員
    And 視窗會關閉、表單會清空，且名單清單會重新查詢

  Scenario: 新增名單時必填欄位未完成不得儲存
    Given 使用者已開啟新增名單視窗
    When 姓名、性別、手機、學歷、來源或等級任一必填欄位未填
    Then 確認儲存按鈕保持不可按

  Scenario: 學歷選擇其他時可輸入自訂學歷內容
    Given 使用者已開啟新增名單視窗
    When 使用者將學歷選為其他
    Then 畫面顯示自訂學歷輸入欄位
    When 使用者改選其他以外的學歷
    Then 自訂學歷內容會同步改成所選學歷文字

  Scenario: 非主管不得執行名單派發
    Given 使用者身份為一般業務
    When 使用者進入電訪總覽頁
    Then 派發按鈕不得顯示為可操作狀態

  Scenario: 使用者編輯既有名單時會先帶入原始資料
    Given 使用者已查出既有名單
    When 使用者按下某筆名單的編輯
    Then 編輯視窗標題會改為編輯名單
    And 畫面會帶入該筆名單原本的區域、學顧、來源、等級、學歷與聯絡資料
    When 使用者完成修改後按下儲存
    Then 這筆名單會被更新
    And 視窗會關閉並重新查詢名單清單

  Scenario: 只有主管或建立者本人可刪除單筆名單
    Given 名單清單中存在多筆資料
    When 使用者查看某筆資料列
    Then 只有主管或該筆資料的建立者本人看得到該列刪除按鈕
    When 可刪除者按下刪除並確認
    Then 該筆名單會被刪除
    And 畫面會重新查詢名單清單

  Scenario: 主管可批次刪除已勾選名單
    Given 使用者身份為主管
    And 主管已勾選至少一筆名單
    When 主管按下批次刪除並確認
    Then 所有勾選名單都會被刪除
    And 畫面會重新查詢名單清單

  Scenario: 關閉新增或編輯視窗時會重設表單
    Given 使用者已開啟新增名單或編輯名單視窗
    When 使用者按下關閉
    Then 視窗模式會恢復為新增名單
    And 視窗中的名單欄位會被清空為初始值
